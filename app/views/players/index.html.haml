%h2 Players

%table.table.table-striped.table-bordered.table-responsive
  %thead
    %tr
      %th First Name
      %th Last Name
      %th Number
      %th
      
  %tbody
    - @players.each do |player|
      %tr
        %td= player.first_name
        %td= player.last_name
        %td= player.number
        %td.text-center
          = link_to 'Show', player, class: 'btn btn-default btn-xs'
          = link_to 'Edit', edit_player_path(player), 
            class: 'btn btn-default btn-xs'
          = link_to 'Destroy', player, method: :delete,
            data: { confirm: 'Are you sure?' },
            class: 'btn btn-danger btn-xs'

= link_to 'New Player', new_player_path, class: 'btn btn-default'
